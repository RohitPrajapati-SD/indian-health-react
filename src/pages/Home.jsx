import React from "react";

const Home = () => {
  return (
    <main>
      <section className="slider" style={{ height: "400px", background: "#ccc", display: "flex", alignItems: "center", justifyContent: "center" }}>
        <h2>Slider Area (Add real slider images here)</h2>
      </section>
      <section style={{ padding: "2rem" }}>
        <h2>Welcome to Indian Health Adviser</h2>
        <p>This is the React version of the homepage.</p>
      </section>
    </main>
  );
};

export default Home;
